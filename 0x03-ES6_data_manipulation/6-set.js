/**
 * setFromArray - returns a Set from an array.
 * @param {*} array - an array to produce a set from.
 * @returns - returns a Set from an array.
 */
export default function setFromArray(array) {
  return new Set(array);
}
